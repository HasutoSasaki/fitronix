# Spec-Kit Development Workflow

**Version**: 1.0.0
**Last Updated**: 2025-12-01

このドキュメントは、spec-kitを使用したフェーズ別開発フローを定義します。

---

## 📋 開発フロー概要

```text
┌─────────────────────────────────────────────────────────────┐
│ Step 1: 仕様策定 (/speckit.specify)                         │
│ → ブランチ: {feature-number}-{feature-name}-spec            │
│ → PR: 設計ドキュメントのみ                                   │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 2: 実装計画 (/speckit.plan)                            │
│ → 同じspecブランチに追加                                     │
│ → 技術調査、データモデル、契約定義                           │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 3: タスク生成 (/speckit.tasks)                         │
│ → 同じspecブランチに追加                                     │
│ → 177タスクをフェーズ別に分解                                │
└─────────────────────────────────────────────────────────────┘

**Note**: フルロードマップ = 177タスク (8+フェーズ)。このドキュメントはMVP配信 (Phase 1-4, 73タスク) にフォーカス。後続フェーズはMVP後に文書化。

                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 4: Spec PR作成・マージ                                 │
│ → PR: 設計ドキュメント完全版                                 │
│ → レビュー: 仕様の妥当性、技術選定、タスク分解               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 5: フェーズ別実装 (/speckit.implement)                 │
│ → 各PhaseごとにブランチとPRを作成                            │
│ → TDD準拠、段階的デリバリー                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🌿 ブランチ戦略

### 1. Specブランチ (設計専用)

**ブランチ名**: `{feature-number}-{feature-name}-spec`

**例**: `001-workout-tracker-spec`

**含まれるファイル**:

```plaintext
specs/{feature-number}-{feature-name}/
├── spec.md              # /speckit.specify 出力
├── plan.md              # /speckit.plan 出力
├── research.md          # /speckit.plan 出力
├── data-model.md        # /speckit.plan 出力
├── contracts/           # /speckit.plan 出力
├── quickstart.md        # /speckit.plan 出力
├── tasks.md             # /speckit.tasks 出力
└── checklists/          # /speckit.plan 出力
```

**PRタイミング**: すべての設計ドキュメント生成後

**PRレビューポイント**:

- [ ] 仕様の完全性 (spec.md)
- [ ] 技術選定の妥当性 (plan.md, research.md)
- [ ] データモデルの正規化 (data-model.md)
- [ ] タスク分解の適切性 (tasks.md)
- [ ] 憲法準拠確認 (plan.md Constitution Check)

---

### 2. Phase別ブランチ (実装専用)

**ブランチ命名規則**:

```plaintext
{feature-number}-{feature-name}-phase{N}-{phase-name}
```

**例**:

- `001-workout-tracker-phase1-setup`
- `001-workout-tracker-phase2-foundation`
- `001-workout-tracker-phase3-us6-navigation`
- `001-workout-tracker-phase4-us1-recording` (MVP完成)

**ブランチ作成元**: `main` (spec PRマージ後)

**1つのPhaseブランチに含まれるタスク**:

- Phase 1: T001-T016 (Setup)
- Phase 2: T017-T038 (Foundational)
- Phase 3: T039-T051 (US6 Navigation)
- Phase 4: T052-T073 (US1 Recording - MVP)
- ...

**PRタイミング**: そのPhaseのすべてのタスク完了後

**PRレビューポイント**:

- [ ] すべてのタスクが完了 (tasks.mdでチェック済み)
- [ ] TDD準拠 (テストが先、実装が後)
- [ ] テストカバレッジ ≥80% (憲法要件)
- [ ] 型安全性 (TypeScript strict mode)
- [ ] アクセシビリティ (WCAG 2.1 AA)
- [ ] パフォーマンス (FCP < 1.5s, TTI < 3s)

---

## 📝 Phase別PR作成ガイド

### Phase 1: Setup (16タスク)

**ブランチ**: `001-workout-tracker-phase1-setup`

**タスク範囲**: T001-T016

**PRタイトル**: `feat(setup): Phase 1 - プロジェクトセットアップ完了`

**PR説明テンプレート**:

```markdown
## 📋 Phase 1: Setup

**タスク範囲**: T001-T016 (16タスク)

### 完了タスク

- [x] T001: React + TypeScript + Vite初期化
- [x] T002: 依存関係インストール
- [x] T003-T005: Zustand, react-window, Vitest
- [x] T006: ESLint + Prettier
- [x] T007-T009: Capacitor初期化、iOS/Android追加
- [x] T010-T011: Preferences/Notifications plugin
- [x] T012-T013: src/, tests/ ディレクトリ構造
- [x] T014-T016: Vitest設定、mocks、TypeScript strict mode

### 作成ファイル

- package.json (React 19, TypeScript 5.7, Vite 6)
- tsconfig.json (strict mode)
- vite.config.ts
- vitest.config.ts
- .eslintrc.cjs
- .prettierrc
- src/ (7ディレクトリ)
- tests/ (4ディレクトリ)
- capacitor.config.ts

### 検証

- [ ] `npm install` 成功
- [ ] `npm run dev` 起動
- [ ] `npm run test` 実行可能
- [ ] `npm run lint` エラーなし

### 次のPhase

Phase 2: Foundational (T017-T038)
```

---

### Phase 2: Foundational (22タスク)

**ブランチ**: `001-workout-tracker-phase2-foundation`

**タスク範囲**: T017-T038

**重要**: ⚠️ このPhaseが完了するまで、ユーザーストーリー実装は開始できません

**PRタイトル**: `feat(foundation): Phase 2 - 基盤レイヤー完了 (TDD)`

**PR説明テンプレート**:

```markdown
## 📋 Phase 2: Foundational

**タスク範囲**: T017-T038 (22タスク)

### TDD フロー

1. **Red Phase** (T024-T027): Contract tests (失敗)
2. **Green Phase** (T028-T030): 仮実装 (テスト通過)
3. **Refactor Phase** (T031-T032): 品質改善

### 完了タスク

**型定義** (T017-T023):

- [x] WorkoutSession, WorkoutExercise, Set
- [x] Exercise, BodyPart, TimerPreset, TimerState

**Storage Layer** (T024-T032):

- [x] Contract tests (Preferences, WorkoutSession, ExerciseLibrary)
- [x] 仮実装 (PreferencesStorage, WorkoutSessionStorage, ExerciseLibraryStorage)
- [x] Refactor (エラーハンドリング、検証)

**Shared UI** (T033-T038):

- [x] Button, InputField, Modal
- [x] LoadingSpinner, EmptyState, ErrorMessage

### テストカバレッジ

- Contract tests: 100% (全インターフェース検証済み)
- Unit tests: ≥80%

### 次のPhase

Phase 3: US6 Navigation (T039-T051) - MVP基盤
```

---

### Phase 3: US6 - Navigation (13タスク)

**ブランチ**: `001-workout-tracker-phase3-us6-navigation`

**タスク範囲**: T039-T051

**PRタイトル**: `feat(us6): Phase 3 - ナビゲーション実装 (MVP基盤)`

**Independent Test**:

> アプリ起動→タブバーで「履歴」タップ→履歴画面表示→「ライブラリ」タップ→ライブラリ画面表示→「記録」タップ→記録画面表示、の流れがスムーズに動作することを確認

---

### Phase 4: US1 - Workout Recording (22タスク) 🎯 MVP

**ブランチ**: `001-workout-tracker-phase4-us1-recording`

**タスク範囲**: T052-T073

**PRタイトル**: `feat(us1): Phase 4 - ワークアウト記録実装 (MVP完成) 🎯`

**Independent Test**:

> ユーザーが新規セッションを開始→部位選択→種目選択→重量とレップ数入力→保存、の一連の流れを完了でき、保存したデータがアプリ再起動後も残っていることを確認できる

**MVP Checkpoint**: ✅ このPhase完了でMVP動作可能

---

## 🚀 実装開始方法

### 1. Spec PRがマージされた後

```bash
# mainに戻る
git checkout main
git pull origin main

# Phase 1ブランチ作成
git checkout -b 001-workout-tracker-phase1-setup

# T001から実装開始 (例: spec-kit CLIまたは手動実装)
# /speckit.implement T001  # 例 - spec-kit CLI利用時
```

### 2. Phase完了後

```bash
# すべてのタスクが完了していることを確認
grep -E "\[x\].*T0(0[1-9]|1[0-6])" specs/001-workout-tracker/tasks.md

# コミット
git add .
git commit -m "feat(setup): Phase 1完了 (T001-T016)"

# Push & PR作成
git push -u origin 001-workout-tracker-phase1-setup
gh pr create --title "feat(setup): Phase 1 - プロジェクトセットアップ完了" --body "..."
```

### 3. 次のPhaseへ

```bash
# mainに戻る (前のPhase PRマージ後)
git checkout main
git pull origin main

# 次のPhaseブランチ作成
git checkout -b 001-workout-tracker-phase2-foundation

# 実装開始 (例: spec-kit CLIまたは手動実装)
# /speckit.implement T017  # 例 - spec-kit CLI利用時
```

---

## 📊 進捗管理

### tasks.mdの更新

タスク完了時に必ず `[ ]` → `[x]` に更新:

```markdown
- [x] T001 Initialize React + TypeScript + Vite project
- [x] T002 Install core dependencies
- [ ] T003 Install Zustand 5 for state management ← 次のタスク
```

### Phase完了チェックリスト

各Phase PR作成前に確認:

- [ ] すべてのタスクが `[x]` になっている
- [ ] テストがすべてパス (`npm run test`)
- [ ] Lintエラーなし (`npm run lint`)
- [ ] ビルド成功 (`npm run build`)
- [ ] テストカバレッジ ≥80% (`npm run test:coverage`)
- [ ] 憲法準拠 (plan.md Constitution Check参照)

---

## 🔄 並列実行のタイミング

### Phase 2完了後

**並列実行可能なPhase**:

- Phase 3: US6 (Navigation)
- Phase 6: US3 (Timer)
- Phase 8: US5 (Library)

**依存関係あり** (順次実行):

- Phase 4: US1 (Recording) - Phase 3の後
- Phase 5: US2 (Keypad) - Phase 4の後
- Phase 7: US4 (History) - Phase 4の後

---

## 📦 MVP デリバリー戦略

### MVP = Phase 1 + 2 + 3 + 4 (73タスク)

**マージ順序**:

1. `001-workout-tracker-spec` → main
2. `001-workout-tracker-phase1-setup` → main
3. `001-workout-tracker-phase2-foundation` → main
4. `001-workout-tracker-phase3-us6-navigation` → main
5. `001-workout-tracker-phase4-us1-recording` → main ✅ **MVP完成**

**MVP完成後のデモ**:

- iOS Simulatorで動作確認
- Android Emulatorで動作確認
- 基本的なワークアウト記録フロー完遂

---

## ❌ アンチパターン

### ❌ BAD: 1つのブランチですべて実装

```bash
# これはやらない
git checkout -b 001-workout-tracker
# T001-T177まで全部実装...
# PR: 10,000行以上の差分 → レビュー不可能
```

### ✅ GOOD: Phase別ブランチで段階的PR

```bash
# Phase 1
git checkout -b 001-workout-tracker-phase1-setup
# T001-T016実装
# PR #2: 200行程度 → レビュー可能

# Phase 2
git checkout -b 001-workout-tracker-phase2-foundation
# T017-T038実装
# PR #3: 400行程度 → レビュー可能
```

---

## 📚 参考ドキュメント

- [Constitution v1.1.0](../.specify/memory/constitution.md)
- [Spec Template](../.specify/templates/spec-template.md)
- [Tasks Template](../.specify/templates/tasks-template.md)
- [Plan Template](../.specify/templates/plan-template.md)

---

**このワークフローに従うことで**:

- ✅ レビュー可能なPRサイズ
- ✅ 段階的なデリバリー
- ✅ 失敗時のロールバック容易
- ✅ 並列開発の機会最大化
- ✅ MVP早期リリース

Happy Coding! 🚀
